<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground Service — จำเป็นเพื่อให้ service รอดจาก Android battery optimizer -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 13+ ต้องขอ POST_NOTIFICATIONS ก่อนแสดง foreground notification -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- ป้องกัน CPU sleep ขณะรอ notification -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Android restart service หลัง reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestNotification">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            NotificationListenerService
            - ต้องเพิ่ม foreground serviceType เพื่อให้รอดจาก Android Doze/OEM killer
            - android:stopWithTask="false" = service ไม่ตายแม้ swipe ออกจาก recent apps
        -->
        <service
            android:name=".service.TestNotificationService"
            android:exported="true"
            android:label="@string/service_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:stopWithTask="false"
            android:foregroundServiceType="dataSync">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
            <!-- กรองเฉพาะ alerting notifications (ธนาคารจะเป็น alerting เสมอ) -->
            <meta-data
                android:name="android.service.notification.default_filter_types"
                android:value="alerting" />
        </service>

        <!-- Restart service หลัง reboot -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
